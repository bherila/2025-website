name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read

    steps:
      - uses: actions/checkout@v6

      - name: Detect changed paths
        uses: dorny/paths-filter@v3
        id: changes
        with:
          filters: |
            frontend:
              - '**/*.ts'
              - '**/*.tsx'
              - '**/*.js'
              - '**/*.jsx'
              - '**/*.css'
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'vite.config.*'
              - 'resources/**'
            backend:
              - '**/*.php'
              - 'composer.json'
              - 'composer.lock'
              - 'phpunit.xml'
              - 'database/**'
              - 'scripts/**'

      - name: Set up pnpm
        if: steps.changes.outputs.frontend == 'true' || github.event_name == 'push'
        uses: pnpm/action-setup@v5
        with:
          version: 10

      - name: Set up Node
        if: steps.changes.outputs.frontend == 'true' || github.event_name == 'push'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install Node dependencies
        if: steps.changes.outputs.frontend == 'true' || github.event_name == 'push'
        run: pnpm install --frozen-lockfile

      - name: Run pnpm type-check (TypeScript)
        if: steps.changes.outputs.frontend == 'true' || github.event_name == 'push'
        run: pnpm run type-check

      - name: Run pnpm lint (ESLint)
        if: steps.changes.outputs.frontend == 'true' || github.event_name == 'push'
        run: pnpm run lint

      - name: Build frontend
        if: steps.changes.outputs.frontend == 'true' || github.event_name == 'push'
        run: pnpm run build

      - name: Run pnpm tests (Jest)
        if: steps.changes.outputs.frontend == 'true' || github.event_name == 'push'
        run: pnpm run test

      - name: Setup PHP
        if: steps.changes.outputs.backend == 'true' || github.event_name == 'push'
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'

      - name: Cache Composer dependencies
        if: steps.changes.outputs.backend == 'true' || github.event_name == 'push'
        uses: actions/cache@v4
        with:
          path: vendor
          key: composer-${{ hashFiles('composer.lock') }}
          restore-keys: composer-

      - name: Install Composer dependencies (with dev)
        if: steps.changes.outputs.backend == 'true' || github.event_name == 'push'
        run: composer install --no-interaction --prefer-dist

      - name: Check MySQL identifier lengths in migrations
        if: steps.changes.outputs.backend == 'true' || github.event_name == 'push'
        run: python3 scripts/check-migration-identifiers.py

      - name: Run PHPUnit
        if: steps.changes.outputs.backend == 'true' || github.event_name == 'push'
        run: vendor/bin/phpunit --configuration phpunit.xml

      - name: Upload frontend build artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: public/build/
          retention-days: 1

  deploy:
    name: Deploy to Production
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: prod

    steps:
      - uses: actions/checkout@v6

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'

      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: vendor
          key: composer-${{ hashFiles('composer.lock') }}
          restore-keys: composer-

      - name: Install Composer (no dev)
        run: composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader

      - name: Download frontend build artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: public/build/

      - name: Append to .htaccess
        run: cat htaccess-append.txt >> public/.htaccess

      - name: Install sshpass
        run: sudo apt-get install -y sshpass

      - name: Deploy with rsync
        run: |
          sshpass -p "${{ secrets.SSH_PASSWORD }}" rsync -av --delete --exclude='.env' \
            -e "ssh -o StrictHostKeyChecking=no" \
            app \
            bootstrap \
            config \
            database \
            public \
            resources \
            routes \
            storage \
            vendor \
            artisan \
            composer.json \
            composer.lock \
            ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}:~/bwh-php/

      - name: Run artisan migrate, config clear/cache on server
        run: |
          sshpass -p "${{ secrets.SSH_PASSWORD }}" ssh -o StrictHostKeyChecking=no \
            ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} \
            "cd ~/bwh-php && /opt/cpanel/ea-php83/root/usr/bin/php artisan migrate --force && /opt/cpanel/ea-php83/root/usr/bin/php artisan config:clear && /opt/cpanel/ea-php83/root/usr/bin/php artisan config:cache"
