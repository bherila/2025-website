import React from 'react'
import ReactDOM from 'react-dom/client'

import ManageAwardsPage from '@/components/rsu/ManageAwardsPage'

ReactDOM.createRoot(document.getElementById('manage-awards-root')!).render(
  <React.StrictMode>
    <ManageAwardsPage />
  </React.StrictMode>
)
